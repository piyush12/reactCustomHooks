import { Canvas, Meta, Story } from "@storybook/addon-docs";
import { Demo } from "./demo.stories";

<Meta title='Side-effect/useAsync' component={Demo} />

# useAsync

Handling async request.

- run function to call the async function
- status will return pending, success or error
- data will return the value returning from async function

#### Example

<Canvas>
  <Story story={Demo} name='useAsync' />
</Canvas>
